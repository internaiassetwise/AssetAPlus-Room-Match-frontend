# Frontend — environment variables
# Copy this file to `.env` for local overrides, or set these in Netlify/Vercel.

# Where the API lives. Leave unset in dev (uses Vite's /api proxy).
# In production, point to your API's public URL.
VITE_API_BASE=